version: '3.8'

services:
  flask-app:
    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: flask-app

    ports:
      - "5000:5000"

    environment:
      MYSQL_HOST: flask-rds-db.cnqimo8iklww.ap-south-1.rds.amazonaws.com
      MYSQL_USER: notesuser
      MYSQL_PASSWORD: Notespass123#
      MYSQL_DB: quicknotes_db

    restart: always